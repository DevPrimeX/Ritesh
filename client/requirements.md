## Packages
framer-motion | Page transitions and animations
embla-carousel-react | For the hero slider and image galleries
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes
lucide-react | Icons (already in base stack, but confirming usage)

## Notes
WhatsApp number set to "919876543210" as requested.
Mock images used via placehold.co.
Admin dashboard requires authentication (handled by /api/user check).
Specs stored as JSONB, handled as dynamic key-value pairs in UI.
